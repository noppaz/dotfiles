#!/bin/zsh

echo "Setting up your Mac..."

# Load env vars (hardcodes .dotfiles since $DOTFILES isn't set yet)
source $HOME/.dotfiles/.zshenv

# Add homebrew to shell
eval "$(/opt/homebrew/bin/brew shellenv)"

# Update Homebrew recipes
brew update

# Install all our dependencies with bundle (See Brewfile)
brew bundle

# Create XDG base directories
for dir in "$XDG_CONFIG_HOME" "$XDG_CACHE_HOME" "$XDG_DATA_HOME" "$XDG_STATE_HOME" "$HOME/.local/bin"; do
  mkdir -p "$dir"
done

# Install Oh My Zsh if not already installed
if [ ! -d "$ZSH" ]; then
  sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)" "" --unattended
fi

# Symlink .zshrc
ln -sfnv $DOTFILES/.zshrc $HOME/.zshrc

# Symlink .zshenv
ln -sfnv $DOTFILES/.zshenv $HOME/.zshenv

# Git config
ln -sfnv $DOTFILES/.gitconfig $HOME/.gitconfig

# Ghostty config
mkdir -p $XDG_CONFIG_HOME/ghostty
ln -sfnv $DOTFILES/ghostty/config $XDG_CONFIG_HOME/ghostty/config

# Zed config
mkdir -p $XDG_CONFIG_HOME/zed
ln -sfnv $DOTFILES/zed/settings.json $XDG_CONFIG_HOME/zed/settings.json

# Neovim config
ln -sfnv $DOTFILES/nvim $XDG_CONFIG_HOME/nvim

# Add Golang folders
mkdir -p $HOME/go
go get golang.org/x/tools/cmd/godoc
go get github.com/golang/lint/golint

# Docker buildx
mkdir -p $DOCKER_CONFIG/cli-plugins
ln -sfnv $(which docker-buildx) $DOCKER_CONFIG/cli-plugins/docker-buildx

# Set macOS preferences - reloads shell
source $DOTFILES/.macos
